# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- feature

pool:
  #vmImage: windows-2019
  name: vardhan-selfhagnt

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'Remove-GitHubRepositoryBranchProtectionRule -OwnerName pamulavardhan -RepositoryName azure-vm-offer-hello-world-202012 -BranchName feature'